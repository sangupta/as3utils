# Property definitions for building this project. 

src.dir=${basedir}/src
test.src.dir=${basedir}/tests
test.application.name=UnitTest
build.libs.dir=${basedir}/build/libs
release.dir=${basedir}/release
report.dir=${release.dir}/reports
report.html.dir=${report.dir}/html
project.name=as3utils
doc.window.title=as3utils | myJerry
FLEX_HOME=C:/FlexSDK/4.1.0.16076AIR2.5.0
